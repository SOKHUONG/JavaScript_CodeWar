function squareSum(numbers){
var newton = numbers.map(power);
 function power(me){
   me= Math.pow(me,2);
   return me;
   return newton;
 }
{ 
var newtun= newton.reduce(sam,0);
   function sam(a,b)
   {
   return a+b;
   }
   return newtun;}


}